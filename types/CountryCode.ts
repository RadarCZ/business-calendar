/**
 * ISO 3166-1 alpha-2 country codes supported by this library.
 *
 * Used to select the appropriate weekend and public-holiday providers
 */
export enum CountryCode {
  /**
   * Czechia (Czech Republic) — code "CZ".
   */
  CZ,
}
